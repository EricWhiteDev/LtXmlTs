Add a new class to the LtXmlTs module.
  - The name of the class is XContainer.
  - It derives from its base class XNode.
  - It should inherit the nodeType and parent properties from the XNode class.
  - It should inherit the addAnnotation(), annotation(), annotations(), and removeAnnotations() methods from the XNode class.
  - It declares a new protected field, nodesArray, which is an array of XNode objects. The nodesArray field can be accessed by code in the XContainer class, and any class that derives from XContainer, but not by any other classes.

XContainer is an abstract base class, so no unit tests are required.

Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints.