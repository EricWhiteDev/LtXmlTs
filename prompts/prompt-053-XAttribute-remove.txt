Enhance the XAttribute class as follows:
- Add a remove() method. This method removes 'this' XAttribute object from its parent's attributesArray.
- First, get the parent XElement object.
- Find 'this' attribute in the parent's attributesArray.
- Use slice to create a new array that consists of all of the attributes before the 'this' XAttribute, and all of the attributes after the 'this' XAttribute
- Replace the attributesArray in the parent with the new array. This effectively removes 'this' XAttribute from the parent XElement
- If the parent property of the XAttribute object is null, then throw a error.

Add unit tests for this functionality.

Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints.
