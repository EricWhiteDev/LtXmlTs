add @category tags to group the standalone functions from XSequence.ts together in the docs.  Name the category "Array Extension Methods".

The external functions that should have the @category tag are:
- ancestors
- ancestorsAndSelf
- attributes
- descendantNodes
- descendants
- descendantsAndSelf
- elements
- inDocumentOrder
- nodes
- remove
- xseq

Note that this @category tag does not apply to the XSequence methods, nor does it apply to methods in XElement, XContainer, etc.  This note about the @category tag only applies to the exported functions in XSequence.ts.

Regenerate the docs after making this change.
