Enhance the XElement class:
- Add a protected method, addAttributeContentList() to the XElement class.
- The addAttributeContentList() method has a rest parameter. The array type of the rest parameter is explicitly unknown.
- In the addAttributeContentList() method, iterate through all arguments in the rest parameter, and call the addAttributeContentObject() method passing each argument in turn.
- Add another protected method, addAttributeContentObject() to the class. This method takes a single argument of type unknown.
- In this addAttributeContentObject() method, process the argument as follows:
  - If the argument is null or undefined, then do nothing with it.
  - If the type of the argument is anything other than XAttribute, then do nothing with it.
  - If the type of the argument is XAttribute, then
    - if the argument has a parent, then clone the XAttribute object by calling the XAttribute clone constructor, then set the parent of the cloned object to the 'this' XElement object, and then add the cloned XAttribute object to the attributesArray. The order in the attributesArray should be the same order as in the rest parameter.
    - if the argument's parent property is null, then set the parent of the XAttribute object to the 'this' XElement object, and then add the XAttribute object to the attributesArray. The order in the attributesArray should be the same order as in the rest parameter.
- Modify the XElement constructor that takes a rest parameter to call addAttributeContentList, passing the rest parameter to it.

Add unit tests as necessary to cause code coverage.

Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints.
