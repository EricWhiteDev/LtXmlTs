Enhance the XElement class as follows:
- Add an ancestorsAndSelf() method.
- This method returns an array of XElement that includes the 'this' XElement, and the element's ancestor elements, in reverse document order.
- The algorithm is as follows:
  - First, create a new array of XElement.
  - Add the 'this' XElement to the array.
  - Add the node's parent to the array.
  - Add the node's parent's parent to the array.
  - And so on until either the parent property is null, or the parent property is an XDocument object.
  - Then return the array.

- Add an overload of the ancestorsAndSelf() method.
  - This method takes one argument, an XName object.
  - This overload is similar to the ancestorsAndSelf method that takes no arguments, except that it returns an array that is filtered to just those XElement objects whose name is the same as the argument XName object.
  - Because XName objects are atomized, then it is sufficient to compare the XName object to the argument XName object.

Add unit tests for this functionality.

Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints.
