Modify XName.getPrefixedName as follows:
- XName.getPrefixedName currently calls getPrefix and unconditionally formats prefix:localName. If getPrefix returns '' (e.g., for an element in the default namespace that has no entry in namespacePrefixPairs), the result would be :localName. This is not handled in this task per the user's spec — it is expected to be addressed separately if needed.
- Therefore, if getPrefix returns '' (e.g. for an element in the default namespace that has no entry in namespacePrefixPairs, or for an attribute in no namespace) then getPrefixedName simply returns the localName.