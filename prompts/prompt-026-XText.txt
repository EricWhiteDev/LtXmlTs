Add a new public class to the LtXmlTs module.
  - The name of the class is XText.
  - It derives from its base class XNode.
  - It declares a readonly field, value, which is a string object.
  - The XText class contains a constructor with two overloads.
    - The first overload has one argument, value, which is a string object. This overload creates a new XText object, setting the value field to the value argument.
    - The second overload has one argument, other, which is an XText object. This is a copy constructor. Set the value field in the newly constructed object to the value field from the other XText object.
  - In the constructor, set the nodeType field to 'Text'.

Ask me questions to clarify the product requirements, technical requirements, engineering principles, and hard constraints.